# Public site URL used for metadata / sitemap. Optional in dev.
NEXT_PUBLIC_SITE_URL=https://launchfold.com

# When you wire up the contact form backend, add its endpoint here and read it
# inside src/hooks/useContactForm.ts (the submit() function is a stub for now).
# CONTACT_ENDPOINT=
